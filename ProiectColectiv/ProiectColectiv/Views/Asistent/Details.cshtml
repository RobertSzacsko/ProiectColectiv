@model ProiectColectiv.Asistent

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_LayoutAsistent.cshtml";
}

<h2>Details</h2>

<div>
    <h4>Asistent</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id_Asistent }) |
    @Html.ActionLink("Back to List", "Index")
</p>
